public String aOrAn(String word) {
    //code here
    String char = word.left(1).tolowercase();
    if(char == 'a' || char == 'e' || char == 'i' || char == 'o' || char == 'u') {
        return 'an ' + word;
    } else {
        return 'a ' + word;
    }
}