public String aOrAn(String word) {
    //code here
    String char = word[0].tolowercase();
    if(char == 'a' || char == 'e' || char == 'i' || char == 'o' || char == 'u') {
        return 'an ' + word;
    } else {
        return 'a ' + word;
    }
}