public String aOrAn(String word) {
    //code here
    String character = word.left(1).tolowercase();
    if(character == 'a' || character == 'e' || character == 'i' || character == 'o' || character == 'u') {
        return 'an ' + word;
    } else {
        return 'a ' + word;
    }
}